# Dependencias de la calculadora de fletes (streamlit_app.py).
# Streamlit Community Cloud instala exactamente esto al desplegar.
#
# Las versiones van FIJAS a proposito: modelo_fletes.joblib se guardo con
# estas, y una version distinta de scikit-learn puede no poder abrirlo o
# cambiar los resultados sin avisar.

streamlit==1.63.0
pandas==3.0.1
numpy==2.4.2
scikit-learn==1.9.0
joblib==1.5.3
